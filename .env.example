# MySQL Configuration
# Copy this file to .env and update the values as needed

# MySQL Root Password (IMPORTANT: Change this in production!)
MYSQL_ROOT_PASSWORD=rootpassword

# Default Database Name
MYSQL_DATABASE=mydatabase

# MySQL User (non-root user)
MYSQL_USER=myuser

# MySQL User Password (IMPORTANT: Change this in production!)
MYSQL_PASSWORD=mypassword

# MySQL Port (default: 3306)
# MYSQL_PORT=3306

# MySQL Host (when connecting from applications)
# MYSQL_HOST=localhost

# phpMyAdmin Port (default: 8080)
# PHPMYADMIN_PORT=8080